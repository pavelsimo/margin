# Kamal secrets — values are read from your environment at deploy time.
# Only $ENV references belong here; NEVER commit literal secrets.

KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
SMTP_USERNAME=$SMTP_USERNAME
SMTP_PASSWORD=$SMTP_PASSWORD
